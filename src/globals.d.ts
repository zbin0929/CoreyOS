/// <reference types="vite/client" />

/**
 * Global constants injected by Vite (see `vite.config.ts` → `define`).
 * Declared here so every import site gets proper typing + IDE hover
 * without a per-file `declare` block.
 */
declare const __APP_VERSION__: string;
